/**
 * @title DQProject - Solana Migration Notice
 * 
 * =====================================================
 * ⚠️  已迁移到 Solana 生态系统 ⚠️
 * =====================================================
 * 
 * 此文件已弃用。请使用 Anchor/Rust 实现。
 * 
 * 新代码位置:
 * - contracts/src/lib.rs     (Anchor 程序)
 * - contracts/Cargo.toml     (Rust 依赖)
 * 
 * =====================================================
 * 
 * 如果你仍需要 BSC/EVM 版本，请参考 git history。
 */

# 迁移说明

## 为什么迁移到 Solana?

| 因素 | BSC/EVM | Solana |
|------|---------|-------|
| 交易费用 | $0.20-$5.00 | $0.001-$0.10 |
| TPS | ~100 | ~65,000 |
| 确认时间 | ~15秒 | ~400ms |
| 开发者体验 | 成熟 | Anchor 框架简化开发 |
| 生态 | PancakeSwap | Raydium, Jupiter |

## 新架构

```
┌─────────────────────────────────────────────────────────────┐
│                      Solana Blockchain                       │
│                                                              │
│  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐    │
│  │ DQProject   │     │ SPL Token   │     │   Raydium   │    │
│  │  Program    │────▶│  (DQ/SOL)   │────▶│    DEX      │    │
│  └─────────────┘     └─────────────┘     └─────────────┘    │
│         │                                         │          │
│         ▼                                         ▼          │
│  ┌─────────────┐                          ┌─────────────┐    │
│  │  Anchor     │                          │  Metaplex   │    │
│  │  Accounts   │                          │  (NFT)      │    │
│  └─────────────┘                          └─────────────┘    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 下一步

1. 查看 `contracts/src/lib.rs` - 新合约代码
2. 查看 `docs/TUTORIAL.md` - Solana 开发教程
3. 查看 `docs/MIGRATION.md` - 详细迁移指南
4. 使用 `anchor build` 构建新合约

---
生成时间: 2024
